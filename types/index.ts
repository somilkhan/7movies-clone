export interface Media { id: number; title?: string; name?: string; poster_path: string | null; backdrop_path: string | null; overview?: string; release_date?: string; first_air_date?: string; vote_average?: number; popularity?: number; media_type?: string; genre_ids?: number[]; logo_path?: string; runtime?: number; genres?: { id: number; name: string }[]; credits?: { cast: { id: number; name: string; character: string; profile_path: string | null }[] }; similar?: { results: Media[] }; videos?: { results: { key: string; type: string; site: string }[] }; images?: { logos: { file_path: string }[] }; number_of_seasons?: number; seasons?: { season_number: number; name: string; episode_count: number; poster_path: string | null }[]; } export interface Movie extends Media { title: string; release_date: string; runtime: number; genres: { id: number; name: string }[]; credits: { cast: { id: number; name: string; character: string; profile_path: string | null }[] }; similar: { results: Media[] }; videos: { results: { key: string; type: string; site: string }[] }; images: { logos: { file_path: string }[] }; } export interface TVShow extends Media { name: string; first_air_date: string; number_of_seasons: number; seasons: { season_number: number; name: string; episode_count: number; poster_path: string | null }[]; credits: { cast: { id: number; name: string; character: string; profile_path: string | null }[] }; similar: { results: Media[] }; videos: { results: { key: string; type: string; site: string }[] }; images: { logos: { file_path: string }[] }; } export interface Season { id: number; name: string; season_number: number; episodes: Episode[]; poster_path: string | null; } export interface Episode { id: number; name: string; episode_number: number; season_number: number; overview: string; still_path: string | null; runtime: number | null; air_date: string; } export interface WatchlistItem { id: number; mediaType: 'movie' | 'tv'; addedAt: string; } export interface ContinueWatchingItem { id: number; type: 'movie' | 'tv'; title: string; poster: string | null; backdrop: string | null; watched: number; duration: number; season?: number; episode?: number; episodeName?: string; timestamp: number; } export type AmbienceTheme = 'dark' | 'midnight' | 'warm'; export interface AppSettings { ambience: AmbienceTheme; reducedMotion: boolean; spoilerProtection: boolean; }